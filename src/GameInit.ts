import { Car } from "./Car";
import { EnemyCar } from "./EnemyCar";
import { Score } from "./Score";
import { Lives } from "./Lives";
import * as PIXI from "pixi.js";

//export class GameInit {
 //   public car!: Car;
  //  public enemyCar!: EnemyCar;
  //  public score!: Score;
  //  public lives!: Lives;

  // public app: PIXI.Application;

   // constructor(app: PIXI.Application) {
   //     this.app = app;
     //   this.initializeNewGame();
   // }

   // public initializeNewGame(): void {
      //  this.car = new Car();
      //  this.score = new Score();
      //  this.lives = new Lives();
       // this.enemyCar = new EnemyCar();
      //  this.app = new PIXI.Application();
     //   this.app.stage.addChild(this.car);
      //  this.app.stage.addChild(this.score);
      //  this.app.stage.addChild(this.lives);
      //  this.app.stage.addChild(this.enemyCar);
  //  }

    // draw(stage: PIXI.Container): void {
    //  stage.addChild(this.car);
    // stage.addChild(this.score);
    // stage.addChild(this.lives);
    // stage.addChild(this.enemyCar);
    // console.log("Draw should load first");
    //}
//}
